# Moongate Dark 编辑器 UI 颜色
# 所有颜色值引用 src/core/colors.yaml 中的变量
# 海拔系统已集成：surfaceGround（地面）、surfaceRaised（隆起）、surfaceFloating（漂浮）、surfaceTooltip（提示）、borderFloating（浮层边框）

# 基础环境
editor.background: "${surfaceGround}"
editor.foreground: "${text}"

# 标题栏（标题栏可略高于编辑器，使用 raised 层）
titleBar.activeBackground: "${surfaceRaised}"
titleBar.activeForeground: "${text}"
titleBar.inactiveBackground: "${surfaceRaised}"
titleBar.inactiveForeground: "${textInactive}"
titleBar.border: "${border}"

# 菜单栏（菜单为浮层，使用 floating 层）
menu.background: "${surfaceFloating}"
menu.foreground: "${text}"
menu.selectionBackground: "${primarySolid}"
menu.selectionForeground: "${white}"
menu.border: "${borderFloating}"

# 活动栏（活动栏为次级区域，使用 raised 层）
activityBar.background: "${surfaceRaised}"
activityBar.foreground: "${text}"
activityBar.inactiveForeground: "${textInactive}"
activityBar.border: "${border}"
activityBar.activeBorder: "${primary}"
activityBarBadge.background: "${primarySolid}"
activityBarBadge.foreground: "${white}"

# 侧边栏（侧边栏为次级区域，使用 raised 层）
sideBar.background: "${surfaceRaised}"
sideBar.foreground: "${text}"
sideBar.border: "${border}"
sideBarTitle.foreground: "${textInactive}"
sideBarSectionHeader.background: "${surfaceGround}" # 节标题可保持地面或略高，这里保留原意使用 ground
sideBarSectionHeader.foreground: "${text}"

# 状态栏（状态栏可保持地面或略高，这里使用 ground，与编辑器背景一致）
statusBar.background: "${surfaceGround}"
statusBar.foreground: "${textDim}"
statusBar.border: "${border}"
statusBar.noFolderBackground: "${surfaceGround}"
statusBar.noFolderForeground: "${textInactive}"
statusBar.debuggingBackground: "${success}"
statusBar.debuggingForeground: "${surfaceGround}"
statusBarItem.activeBackground: "${bgHover}" # 保持原变量（半透明）
statusBarItem.hoverBackground: "${primary}10" # 保持原变量
statusBarItem.hoverForeground: "${text}"
statusBarItem.prominentBackground: "${surfaceRaised}"
# prominent 为隆起/白底实面：必须显式给前景（缺省会回退 VS Code 默认白字 → 浅色下白压白 1.00:1）
statusBarItem.prominentForeground: "${text}"
statusBarItem.prominentHoverBackground: "${hoverBg}"
statusBarItem.prominentHoverForeground: "${text}"
statusBarItem.remoteBackground: "${surfaceGround}"
statusBarItem.remoteForeground: "${textInactive}"

# 选项卡（选项卡栏为次级区域，使用 raised；选项卡本身背景为地面）
tab.activeBackground: "${surfaceGround}"
tab.activeForeground: "${text}"
tab.activeBorder: "${primary}"
tab.inactiveBackground: "${surfaceRaised}"
tab.inactiveForeground: "${textInactive}"
tab.border: "${border}"
tab.hoverBackground: "${hoverBg}"
tab.hoverForeground: "${text}"

# 编辑器组
editorGroup.border: "${border}"
editorGroupHeader.tabsBackground: "${surfaceRaised}"

# 面板（面板使用地面，保持沉浸）
panel.background: "${surfaceGround}"
panel.border: "${border}"
panelTitle.activeBorder: "${primary}"
panelTitle.activeForeground: "${text}"
panelTitle.inactiveForeground: "${textInactive}"

# 输入框（输入框通常为浮层，使用 floating 层）
input.background: "${surfaceFloating}"
input.foreground: "${text}"
input.border: "${borderFloating}"
input.placeholderForeground: "${textInactive}"
inputOption.activeBackground: "${primarySolid}"
inputOption.activeForeground: "${white}"
inputValidation.errorBackground: "${error}"
inputValidation.errorBorder: "${error}"
# 彩色实底上的文字统一用 surfaceGround（与 statusBarItem.errorForeground 同一惯例）：
# 深色 = 墨色 on 亮色族、浅色 = 近白 on 深色族，两模式均 ≥4.5:1。
# 缺省会回退 VS Code 默认前景（深 1.10–2.00:1 / 浅 1.87–3.24:1，提示不可读）。
inputValidation.errorForeground: "${surfaceGround}"
inputValidation.infoBackground: "${primary}"
inputValidation.infoBorder: "${primary}"
inputValidation.infoForeground: "${surfaceGround}"
inputValidation.warningBackground: "${warning}"
inputValidation.warningBorder: "${warning}"
inputValidation.warningForeground: "${surfaceGround}"

# 下拉框（下拉框为浮层，使用 floating 层）
dropdown.background: "${surfaceFloating}"
dropdown.foreground: "${text}"
dropdown.border: "${borderFloating}"

# 按钮（按钮背景使用主色，前景白色）
button.background: "${primarySolid}"
button.foreground: "${white}"
button.hoverBackground: "${buttonHoverBg}"
button.secondaryBackground: "${surfaceRaised}"
button.secondaryForeground: "${text}"

# 徽章
badge.background: "${primarySolid}"
badge.foreground: "${white}"

# 滚动条
scrollbar.shadow: "${surfaceGround}"
scrollbarSlider.background: "${scrollbar}60"
scrollbarSlider.hoverBackground: "${scrollbar}80"
scrollbarSlider.activeBackground: "${scrollbar}"

# 列表/树（列表项悬停/选中保持原有交互色，背景使用 raised 层）
list.activeSelectionBackground: "${selectedBg}"
list.activeSelectionForeground: "${selectionForeground}"
list.hoverBackground: "${hoverBg}"
list.hoverForeground: "${text}"
list.focusBackground: "${selectedBg}"
list.focusForeground: "${selectionForeground}"
list.inactiveSelectionBackground: "${surfaceRaised}"
list.inactiveSelectionForeground: "${textInactive}"
list.highlightForeground: "${primary}"
list.errorForeground: "${error}"
list.warningForeground: "${warning}"

# 树
tree.indentGuidesStroke: "${border}"

# 快速选择器（浮层，使用 floating 层）
quickInput.background: "${surfaceFloating}"
quickInput.foreground: "${text}"
quickInputTitle.background: "${surfaceGround}" # 标题可略低，使用 ground
pickerGroup.border: "${border}"
pickerGroup.foreground: "${textInactive}"

# 编辑器细节（保持不变）
editorLineNumber.foreground: "${textInactive}"
editorLineNumber.activeForeground: "${text}"
editorCursor.foreground: "${primary}"
editor.selectionBackground: "${primary}40"
editor.selectionHighlightBorder: "${primary}80"
editor.selectionHighlightBackground: "${primary}20"
editor.findMatchBackground: "${primary}40"
editor.findMatchBorder: "${primary}"
editor.findMatchHighlightBackground: "${primary}20"
editor.findMatchHighlightBorder: "${primary}00"
editor.findRangeHighlightBackground: "${primary}10"
editor.findRangeHighlightBorder: "${primary}00"
editor.wordHighlightBackground: "${primary}30"
editor.wordHighlightStrongBackground: "${primary}50"
editorWhitespace.foreground: "${border}"
editorIndentGuide.background1: "${border}"
editorIndentGuide.activeBackground1: "${primary}"
editorRuler.foreground: "${border}"
editorCodeLens.foreground: "${textInactive}"
editorBracketMatch.background: "${primary}30"
editorBracketMatch.border: "${primary}"
editorOverviewRuler.border: "${border}"
editorOverviewRuler.modifiedForeground: "${warning}"
editorOverviewRuler.addedForeground: "${success}"
editorOverviewRuler.deletedForeground: "${error}"
editorError.foreground: "${error}"
editorWarning.foreground: "${warning}"
editorInfo.foreground: "${primary}"
editorHint.foreground: "${textInactive}"
editorError.border: "${error}80"
editorWarning.border: "${warning}80"

# 差异编辑器
diffEditor.insertedTextBackground: "${success}20"
diffEditor.removedTextBackground: "${error}20"
diffEditor.border: "${border}"

# 面包屑
breadcrumb.background: "${surfaceGround}"
breadcrumb.foreground: "${textInactive}"
breadcrumb.focusForeground: "${text}"
breadcrumbPicker.background: "${surfaceFloating}" # 面包屑选择器为浮层

# 编辑器小部件（建议小部件、悬停小部件等浮层）
editorWidget.background: "${surfaceFloating}"
editorWidget.foreground: "${text}"
editorWidget.border: "${borderFloating}"
editorSuggestWidget.background: "${surfaceFloating}"
editorSuggestWidget.border: "${borderFloating}"
editorSuggestWidget.foreground: "${text}"
editorSuggestWidget.highlightForeground: "${primary}"
editorSuggestWidget.selectedBackground: "${selectedBg}"
editorSuggestWidget.selectedForeground: "${selectionForeground}"
editorHoverWidget.background: "${surfaceTooltip}" # 悬停提示用最高层
editorHoverWidget.foreground: "${text}"
editorHoverWidget.border: "${borderFloating}"

# 调试（保持不变）
debugToolBar.background: "${surfaceRaised}"
debugToolBar.border: "${border}"
debugIcon.startForeground: "${debugStart}"
debugIcon.pauseForeground: "${debugPause}"
debugIcon.stopForeground: "${debugStop}"
debugIcon.disconnectForeground: "${error}"
debugIcon.continueForeground: "${debugStart}"
debugIcon.breakpointForeground: "${error}"
debugIcon.breakpointDisabledForeground: "${textInactive}"

# Git 装饰（保持不变）
gitDecoration.addedResourceForeground: "${gitAdded}"
gitDecoration.modifiedResourceForeground: "${gitModified}"
gitDecoration.deletedResourceForeground: "${gitDeleted}"
gitDecoration.untrackedResourceForeground: "${gitUntracked}"
gitDecoration.ignoredResourceForeground: "${gitIgnored}"
gitDecoration.conflictingResourceForeground: "${gitModified}"

# 内嵌提示（保持不变）
editorInlayHint.foreground: "${punctuation}"
editorInlayHint.background: "${surfaceRaised}40" # 半透明背景，使用 raised 层
editorInlayHint.typeForeground: "${punctuation}"
editorInlayHint.parameterForeground: "${punctuation}"

# 终端
terminal.background: "${surfaceGround}"
terminal.foreground: "${text}"
terminalCursor.foreground: "${primary}"
# 光标块上的字符色（与光标块成对，须 ≥4.5:1；深 4.85:1 / 浅 8.35:1）
terminalCursor.background: "${surfaceGround}"
terminal.selectionBackground: "${primary}40"
terminal.border: "${border}"
terminal.ansiBlack: "${ansiBlack}"
terminal.ansiRed: "${ansiRed}"
terminal.ansiGreen: "${ansiGreen}"
terminal.ansiYellow: "${ansiYellow}"
terminal.ansiBlue: "${ansiBlue}"
terminal.ansiMagenta: "${ansiMagenta}"
terminal.ansiCyan: "${ansiCyan}"
terminal.ansiWhite: "${ansiWhite}"
terminal.ansiBrightBlack: "${ansiBrightBlack}"
terminal.ansiBrightRed: "${ansiBrightRed}"
terminal.ansiBrightGreen: "${ansiBrightGreen}"
terminal.ansiBrightYellow: "${ansiBrightYellow}"
terminal.ansiBrightBlue: "${ansiBrightBlue}"
terminal.ansiBrightMagenta: "${ansiBrightMagenta}"
terminal.ansiBrightCyan: "${ansiBrightCyan}"
terminal.ansiBrightWhite: "${ansiBrightWhite}"

# 通知（浮层）
notifications.background: "${surfaceFloating}"
notifications.foreground: "${text}"
notifications.border: "${borderFloating}"

# 扩展
extensionButton.prominentBackground: "${primarySolid}"
extensionButton.prominentForeground: "${white}"
extensionButton.prominentHoverBackground: "${buttonHoverBg}"

# 进度条
progressBar.background: "${primary}"

# 括号高亮（保持不变）
editorBracketHighlight.foreground1: "${bracket1}"
editorBracketHighlight.foreground2: "${bracket2}"
editorBracketHighlight.foreground3: "${bracket3}"
editorBracketHighlight.foreground4: "${bracket4}"
editorBracketHighlight.foreground5: "${bracket5}"
editorBracketHighlight.foreground6: "${bracket6}"
editorBracketHighlight.unexpectedBracket.foreground: "${error}"

# 滚动粘滞标题（Sticky Scroll）
editorStickyScroll.background: "${surfaceRaised}"
editorStickyScroll.border: "${border}"
editorStickyScrollHover.background: "${hoverBg}"

# AI 聊天（Chat / Inline Chat）
chat.requestBackground: "${bgMuted}"
chat.requestBorder: "${border}"
chat.avatarBackground: "${primary}"
chat.avatarForeground: "${white}"
chat.slashCommandBackground: "${primary}20"
chat.slashCommandForeground: "${text}"
inlineChat.background: "${surfaceFloating}"
inlineChat.border: "${borderFloating}"
inlineChatInput.background: "${surfaceRaised}"
inlineChatInput.border: "${border}"
inlineChatInput.placeholderForeground: "${textInactive}"

# 命令中心（Command Center）
commandCenter.background: "${surfaceGround}"
commandCenter.activeBackground: "${primary}20"
commandCenter.border: "${border}"
commandCenter.foreground: "${text}"
commandCenter.activeForeground: "${text}"
commandCenter.inactiveForeground: "${textInactive}"

# 终端命令指南（Terminal Command Guide）
terminalCommandGuide.foreground: "${text}"

# 编辑器 Ghost Text / Unicode 高亮
editorGhostText.background: "${surfaceFloating}"
editorGhostText.border: "${borderFloating}"
editorUnicodeHighlight.border: "${warning}"
editorUnicodeHighlight.background: "${warning}20"

# 注释线程（Comments）/ 端口（Ports）
commentsView.resolvedIcon: "${success}"
commentsView.unresolvedIcon: "${warning}"

# 符号图标（Symbol Icons）
symbolIcon.arrayForeground: "${warning}"
symbolIcon.booleanForeground: "${primary}"
symbolIcon.classForeground: "${warning}"
symbolIcon.colorForeground: "${purple}"
symbolIcon.constantForeground: "${warning}"
symbolIcon.constructorForeground: "${function}"
symbolIcon.enumeratorForeground: "${warning}"
symbolIcon.enumeratorMemberForeground: "${warning}"
symbolIcon.eventForeground: "${warning}"
symbolIcon.fieldForeground: "${textDim}"
symbolIcon.fileForeground: "${textMuted}"
symbolIcon.folderForeground: "${textMuted}"
symbolIcon.functionForeground: "${function}"
symbolIcon.interfaceForeground: "${primary}"
symbolIcon.keyForeground: "${warning}"
symbolIcon.keywordForeground: "${primary}"
symbolIcon.methodForeground: "${function}"
symbolIcon.moduleForeground: "${warning}"
symbolIcon.namespaceForeground: "${warning}"
symbolIcon.nullForeground: "${textMuted}"
symbolIcon.numberForeground: "${warning}"
symbolIcon.objectForeground: "${warning}"
symbolIcon.operatorForeground: "${operator}"
symbolIcon.packageForeground: "${warning}"
symbolIcon.propertyForeground: "${textDim}"
symbolIcon.referenceForeground: "${textInactive}"
symbolIcon.snippetForeground: "${warning}"
symbolIcon.stringForeground: "${success}"
symbolIcon.structForeground: "${warning}"
symbolIcon.textForeground: "${textMuted}"
symbolIcon.typeParameterForeground: "${cyan}"
symbolIcon.unitForeground: "${warning}"
symbolIcon.variableForeground: "${textDim}"

# ========== A 级：Chat / Copilot 扩展（VS Code 1.119+）==========
chat.editedFileForeground: "${textMuted}"
chat.linesAddedForeground: "${success}"
chat.linesRemovedForeground: "${error}"
chat.requestBubbleBackground: "${bgMuted}"
chat.requestBubbleHoverBackground: "${hoverBg}"
chat.thinkingShimmer: "${primary}40"
chat.checkpointSeparator: "${border}"
chatManagement.sashBorder: "${border}"
aiCustomizationManagement.sashBorder: "${border}"
agentSessionReadIndicator.foreground: "${primary}"
agentStatusIndicator.background: "${primary}20"

# ========== A 级：新编辑器功能 ==========
editorLightBulb.foreground: "${warning}"
editorLightBulbAi.foreground: "${purple}"
editorLightBulbAutoFix.foreground: "${cyan}"
editorMultiCursor.primary.background: "${primary}20"
editorMultiCursor.primary.foreground: "${primary}"
editorMultiCursor.secondary.background: "${primary}10"
editorMultiCursor.secondary.foreground: "${primary}"
editorUnnecessaryCode.border: "${textMuted}80"
editorUnnecessaryCode.opacity: "${codeDim}"

# ========== A 级：Sticky Scroll 新变体（VS Code 1.120+）==========
editorStickyScrollGutter.background: "${surfaceRaised}"
outputViewStickyScroll.background: "${surfaceRaised}"
panelStickyScroll.background: "${surfaceRaised}"
panelStickyScroll.border: "${border}"
panelStickyScroll.shadow: "${surfaceGround}"
sideBarStickyScroll.background: "${surfaceRaised}"
sideBarStickyScroll.border: "${border}"
sideBarStickyScroll.shadow: "${surfaceGround}"
terminalStickyScroll.background: "${surfaceRaised}"
terminalStickyScroll.border: "${border}"
terminalStickyScrollHover.background: "${hoverBg}"
peekViewEditorStickyScroll.background: "${surfaceRaised}"

# ========== A 级：状态栏项状态色 ==========
statusBarItem.errorBackground: "${error}"
statusBarItem.errorForeground: "${surfaceGround}"
statusBarItem.errorHoverBackground: "${error}80"
statusBarItem.errorHoverForeground: "${surfaceGround}"
statusBarItem.warningBackground: "${warning}"
statusBarItem.warningForeground: "${surfaceGround}"
statusBarItem.warningHoverBackground: "${warning}80"
statusBarItem.warningHoverForeground: "${surfaceGround}"
statusBarItem.offlineBackground: "${textMuted}"
statusBarItem.offlineForeground: "${surfaceGround}"

# ========== A 级：选项卡选中态（VS Code 1.126+）==========
tab.selectedBackground: "${selectedBg}"
tab.selectedBorderTop: "${primary}"
tab.selectedForeground: "${selectionForeground}"

# ========== B 级：常见 UI 组件 ==========
banner.background: "${primary}20"
banner.foreground: "${text}"
banner.iconForeground: "${primary}"
checkbox.selectBackground: "${selectedBg}"
radio.activeBackground: "${selectedBg}"
radio.activeBorder: "${primary}"
radio.inactiveBackground: "${bgMuted}"
radio.inactiveHoverBackground: "${hoverBg}"
sash.hoverBorder: "${primary}"

# 面板 / Peek 视图 / 调试控制台
panel.dropBorder: "${border}"
panelSection.border: "${border}"
panelSection.dropBackground: "${primary}20"
panelSectionHeader.background: "${surfaceRaised}"
panelSectionHeader.border: "${border}"
panelSectionHeader.foreground: "${text}"
peekView.border: "${borderFloating}"
peekViewTitle.background: "${surfaceRaised}"
peekViewEditor.background: "${surfaceGround}"
debugConsole.errorForeground: "${error}"
debugConsole.infoForeground: "${primary}"
debugConsole.warningForeground: "${warning}"

# 编辑器缩进线槽 / 代码行槽
editorGutter.background: "${surfaceGround}"
editorGutter.modifiedBackground: "${warning}"
editorGutter.addedBackground: "${success}"
editorGutter.deletedBackground: "${error}"
editorGutter.foldingControlForeground: "${textInactive}"

# 输出视图 / 终端命令装饰
outputView.background: "${surfaceGround}"
terminalCommandDecoration.defaultBackground: "${textInactive}"
terminalCommandDecoration.successBackground: "${success}"
terminalCommandDecoration.errorBackground: "${error}"
terminal.tab.activeBorder: "${primary}"

# 通用小部件
widget.border: "${borderFloating}"
widget.shadow: "${surfaceGround}"
selection.background: "${primary}40"

# ==================== 覆盖补全 2026-09 ====================
# 依据 docs/COVERAGE.md（对照 VS Code 默认主题键集）补齐高可见度键。
# 未定义时会回退 VS Code 默认色：虽自洽，但与月庭色调不一致（且"只定义一半"的配对会不可读）。
# 映射规则：能复用既有语义角色就复用，不新增角色。

# ---- 编辑器高亮层（当前行 / 范围 / 悬停 / 非活跃选区）----
editor.lineHighlightBackground: "${bgElevated}"
editor.rangeHighlightBackground: "${primary}10"
editor.hoverHighlightBackground: "${primary}10"
editor.inactiveSelectionBackground: "${primary}20"

# ---- 缩进参考线（现代键名；background1/activeBackground1 为已废弃别名，两者都保留）----
editorIndentGuide.background: "${border}"
editorIndentGuide.activeBackground: "${primary}"

# ---- 概览标尺 ----
editorOverviewRuler.errorForeground: "${error}"
editorOverviewRuler.warningForeground: "${warning}"
editorOverviewRuler.findMatchForeground: "${highlight}"

# ---- Diff 行底色 ----
diffEditor.insertedLineBackground: "${success}22"
diffEditor.removedLineBackground: "${error}22"
diffEditor.unchangedRegionBackground: "${bgMuted}"

# ---- 选项卡未聚焦态 ----
tab.unfocusedActiveBackground: "${surfaceGround}"
tab.unfocusedActiveForeground: "${textInactive}"
tab.unfocusedInactiveBackground: "${surfaceRaised}"
tab.unfocusedInactiveForeground: "${textMuted}"
tab.unfocusedHoverBackground: "${hoverBg}"
tab.unfocusedActiveBorder: "${border}"
tab.unfocusedActiveBorderTop: "${border}"
tab.activeBorderTop: "${primary}"
tab.lastPinnedBorder: "${border}"

# ---- 列表（焦点描边 / 拖放 / 非法项 / 选中图标）----
list.focusOutline: "${primary}"
list.focusAndSelectionOutline: "${primary}"
list.dropBackground: "${bgHover}"
list.invalidItemForeground: "${error}"
list.activeSelectionIconForeground: "${selectionForeground}"

# ---- Markdown / hover 文档渲染面 ----
textLink.foreground: "${primary}"
textLink.activeForeground: "${highlight}"
textPreformat.foreground: "${warning}"
textPreformat.background: "${bgMuted}"
textBlockQuote.background: "${bgMuted}"
textBlockQuote.border: "${border}"

# ---- 通用文本 ----
descriptionForeground: "${textMuted}"
disabledForeground: "${textInactive}"

# ---- Git 暂存态 ----
gitDecoration.stageModifiedResourceForeground: "${gitModified}"
gitDecoration.stageDeletedResourceForeground: "${gitDeleted}"

# ---- 小地图滑块 ----
minimapSlider.background: "${scrollbar}60"
minimapSlider.hoverBackground: "${scrollbar}80"
minimapSlider.activeBackground: "${scrollbar}"

# ---- 菜单 / 菜单栏 / 工具栏 / 分组头 ----
menu.selectionBorder: "${borderFloating}"
menu.separatorBackground: "${border}"
menubar.selectionBackground: "${primarySolid}"
menubar.selectionForeground: "${white}"
toolbar.hoverBackground: "${hoverBg}"
toolbar.activeBackground: "${bgActive}"
editorGroupHeader.tabsBorder: "${border}"
actionBar.toggledBackground: "${bgActive}"

# ---- 面包屑 / 下拉 / 活动栏 / 建议控件 ----
breadcrumb.activeSelectionForeground: "${text}"
dropdown.listBackground: "${surfaceFloating}"
activityBar.activeBackground: "${bgHover}"
activityBar.activeFocusBorder: "${primary}"
editorSuggestWidget.focusOutline: "${primary}"
editorSuggestWidget.selectedIconForeground: "${selectionForeground}"

# ---- 通知中心 / 欢迎页 / 评论控件 / 笔记本 / 复选框 / 按钮边框 ----
notificationCenterHeader.background: "${surfaceGround}"
notificationCenterHeader.foreground: "${text}"
welcomePage.progress.foreground: "${primary}"
welcomePage.tileBackground: "${surfaceRaised}"
editorCommentsWidget.rangeBackground: "${primary}10"
editorCommentsWidget.rangeActiveBackground: "${primary}20"
notebook.cellBorderColor: "${border}"
notebook.selectedCellBackground: "${bgMuted}"
checkbox.background: "${surfaceFloating}"
checkbox.border: "${borderFloating}"
checkbox.foreground: "${text}"
button.border: "${borderFloating}"
button.secondaryBorder: "${border}"
button.secondaryHoverBackground: "${hoverBg}"
statusBarItem.focusBorder: "${primary}"
statusBarItem.compactHoverBackground: "${primary}20"

# ---- 现代活动栏 / 统一表面 / AI 面板（Agents）----
surface.background: "${surfaceGround}"
surface.foreground: "${text}"
surface.border: "${border}"
modernActivityBar.background: "${surfaceRaised}"
modernActivityBar.activeBackground: "${bgHover}"
modernActivityBar.activeForeground: "${text}"
modernActivityBar.hoverBackground: "${hoverBg}"
modernActivityBar.hoverForeground: "${text}"
commandCenter.activeBorder: "${primary}"
activityBarTop.activeBorder: "${primary}"
agents.background: "${surfaceRaised}"
agentsPanel.background: "${surfaceFloating}"
agentsPanel.foreground: "${text}"
agentsPanel.border: "${borderFloating}"
agentsChatInput.background: "${surfaceFloating}"
agentsChatInput.foreground: "${text}"
agentsChatInput.border: "${borderFloating}"
agentsChatInput.focusBorder: "${primary}"
agentsChatInput.placeholderForeground: "${textInactive}"
agentsNewSessionButton.background: "${primarySolid}"
agentsNewSessionButton.foreground: "${white}"
agentsNewSessionButton.border: "${borderFloating}"
agentsNewSessionButton.hoverBackground: "${buttonHoverBg}"
agentsBadge.background: "${primarySolid}"
agentsBadge.foreground: "${white}"
agentsUnreadBadge.background: "${primarySolid}"
agentsUnreadBadge.foreground: "${white}"
agentsCard.border: "${border}"
agentsBottomPanel.border: "${border}"
agentsGradient.tintColor: "${primary}20"
chat.inputWorkingBorderColor1: "${primary}"
chat.inputWorkingBorderColor2: "${cyan}"
chat.inputWorkingBorderColor3: "${purple}"

# ---- 活动栏问题徽章（与状态栏错误/警告项同一惯例：墨字 on 彩色实底）----
activityErrorBadge.background: "${error}"
activityErrorBadge.foreground: "${surfaceGround}"
activityWarningBadge.background: "${warning}"
activityWarningBadge.foreground: "${surfaceGround}"

# ---- 核心单键补全（全局前景/焦点/图标/通知图标等高频回退点）----
foreground: "${text}"
errorForeground: "${error}"
focusBorder: "${primary}"
icon.foreground: "${textMuted}"
inputOption.activeBorder: "${primary}"
keybindingLabel.foreground: "${textMuted}"
listFilterWidget.shadow: "${surfaceGround}"
notificationCenter.border: "${border}"
notificationToast.border: "${border}"
notificationLink.foreground: "${primary}"
notificationsErrorIcon.foreground: "${error}"
notificationsWarningIcon.foreground: "${warning}"
notificationsInfoIcon.foreground: "${primary}"
problemsWarningIcon.foreground: "${warning}"
panelInput.border: "${border}"
editorLink.activeForeground: "${primary}"
editorStickyScroll.shadow: "${surfaceGround}"
ports.iconRunningProcessForeground: "${success}"

# ---- 覆盖率测试补齐的核心单键（COVERAGE.md）----
textCodeBlock.background: "${bgMuted}"
textSeparator.foreground: "${border}"
sideBarSectionHeader.border: "${border}"
statusBar.focusBorder: "${primary}"
terminal.inactiveSelectionBackground: "${primary}20"
